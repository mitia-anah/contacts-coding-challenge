export function SearchBar() {
  return (
    <form>
      <input type='text' name='names' placeholder='' />
    </form>
  )
}
// filter(
//   (contact) =>
//     contact.first_name.includes(searchText) ||
//     contat.last_name.includes(searchText)
// )
